<h2>Add a new dose</h2>

<%= simple_form_for [@dose @ingredient @cocktail] do |f| %>
  <%= f.input :description %>
  <%= f.input :ingredient_id %>
  <%= f.input :cocktail_id %>
  <%= f.submit "add a dose", class: "btn btn-primary" %>
<% end %>
